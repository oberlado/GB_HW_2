package server;

/*import java.sql.*;

public class blacklist {
    private static Connection connection;

    public static int addUser(String login, String nickname) {
        try {
            String query = "INSERT INTO users (id,nickname,block) VALUES (?, ?, true);";
            PreparedStatement ps = connection.prepareStatement(query);
            ps.setString(1, id);

            return ps.executeUpdate();
        } catch (SQLException e) {
            e.printStackTrace();
        }
        return 0;
    }
}*/

